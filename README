运行需要环境下需要执行：pip install -r requirements.txt  安装第三方库,pip install -r requirements.txt -i https://pypi.douban.com/simple/
python在linux运行托管给docker需要打包成镜像使用Dockerfile:
docker build --no-cache=true -t  device-sim .
docker后台运行容器：docker run -d id

# crc16协议加密
# Linux下找不到文件路径加上下面的注解
# import sys
# sys.path.append('/home/pythonWork/deviceSimulator')

如果只需要运行：deviceSimulator/deviceRuler/deviceLogic.py，则只需要运行：pip install -r device_simulator_require.txt